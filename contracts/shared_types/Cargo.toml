[package]
name = "shared_types"
version.workspace = true
edition = "2021"
license = "MIT"

[lib]
path = "lib.rs"
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
